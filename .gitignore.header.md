---
fileId: contextrail-template:root:gitignore
module: root
stability: evolving
steward: shared
api: Git ignore rules
dependsOn:
  - git
  - .backups/
  - node_modules/
  - generated outputs
summary: Keep local artifacts, dependencies, and generated outputs out of version control without hiding real repository problems.
owns: The ignore policy for local artifacts, dependencies, and generated outputs that should never clutter normal commits.
boundaries: This file defines ignore scope only. It must not become a hidden policy document or a workaround for generated-file drift elsewhere in the repository.
invariants: Ignore patterns stay minimal, intentional, and aligned with current generated outputs and local-only directories.
risks: Over-broad patterns can hide important files; missing patterns can leak generated artifacts into commits.
securityPrivacy: Keep secrets out of the repository rather than relying on ignore rules alone.
notesForLLM: Prefer the smallest possible ignore surface. Do not hide repository issues behind broad globs.
tests: Git status and repository hygiene checks
linkedDocs: README.md
related:
  - .backups/
  - node_modules/
---

# .gitignore
