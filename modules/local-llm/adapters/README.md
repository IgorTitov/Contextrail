<!-- @HEADER
@version 0.7.50 | 2026-05-03
@purpose Document the adapter implementations for the local-llm bounded module: WebLLM (WebGPU) and Transformers.js (WASM) adapters satisfying both LocalLlmPort and AiChatPort.
@sidecar README.md.header.md
@layer module | @hex adapter | @ctx local-llm
@public false
@edit careful -->

# adapters

Adapter implementations for the local-llm bounded module.

Contains WebLLM (WebGPU) and Transformers.js (WASM) adapters that conform to both LocalLlmPort and AiChatPort.
